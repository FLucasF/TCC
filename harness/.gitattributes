# The install scripts are POSIX sh and must keep LF in the working tree on every
# platform. With core.autocrlf=true a Windows checkout would rewrite them to CRLF,
# and the carriage return ends up inside the values: the pinned version becomes
# "0.9.56\r" and the install fails on a version that does not exist.
*.sh text eol=lf
