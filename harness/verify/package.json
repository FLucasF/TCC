{
  "name": "harness-verify",
  "private": true,
  "type": "module",
  "description": "Development dependencies of the executor itself. Nothing here is needed to RUN the harness — runner.mjs uses only Node's standard library. These two exist so the JSDoc types can be checked: `npm install` here, then the harness boundary's typecheck stops reporting BLOCKED.",
  "scripts": {
    "test": "node --test \"*.test.mjs\"",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.3"
  }
}
