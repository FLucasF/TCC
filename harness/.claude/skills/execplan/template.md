# <Short, action-oriented description>

This is a living document, maintained under `.claude/skills/execplan/SKILL.md`.
`Progress`, `Surprises & Discoveries`, `Decision Log` and `Outcomes &
Retrospective` are kept current as the work proceeds, not filled in at the end.

## Purpose

What someone can do after this change that they could not do before, and how they
can see it working. A few sentences, user-visible behavior.

## Progress

- [x] (2026-09-06 13:00) Example completed step.
- [ ] Example remaining step.
- [ ] Example partial step (done: X; remaining: Y).

Timestamps are here to show the rate of progress, so a later reader can tell a
stalled plan from a slow one.

## Surprises & Discoveries

- Observation: ...
  Evidence: ...

## Decision Log

- Decision: ...
  Rationale: ...
  Date: ...

## Outcomes & Retrospective

What was achieved, what remains, what was learned — measured against Purpose.

## Context and Orientation

The current state, written for a reader who knows nothing about this repository.
Key files by full path from the repository root. Every non-obvious term defined
where it first appears.

## Plan of Work

In prose: the sequence of edits, each naming the file and the function or module,
and what changes there.

## Milestones

One section per milestone, each a paragraph: goal, work, result, proof. Each
leaves something working and verifiable on its own.

## Concrete Steps

Exact commands and the directory they run in, with a short expected transcript
where the output is what proves the step worked.

## Validation and Acceptance

How to exercise the system and what to observe, as behavior with specific inputs
and outputs. For tests: the project's own test command, the expected result, and
the name of the test that fails before the change and passes after.

## Interfaces and Dependencies

The libraries, modules and services to use and why. The types, interfaces and
signatures that must exist when the milestone ends, by stable path.

## Revisions

- (date) What changed in this plan and why.
